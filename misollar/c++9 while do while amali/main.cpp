#include <iostream>

using namespace std;

int main()
{
    /*
    //while operatori     //While opratorida oldin shart bajariladi so'ngra harakat bajariladi
    int i = 0;
    while (shart) {
        harakat
    }
    */


    /*
    int i = 1;
    while (i <= 10){
        cout << i << endl;
        i++;
    }
   cout << "Ohirgi qiymat " << i << endl;
   */









    //do while
    //bu qanday ishlaydi oldin amalni bir marta tekshirib koradi songra shartni bajariladi
    int n,m,op;
    /*
    do{
       cout << "N = "; cin >> n;
    }while (n >= 0);

    cout << "Hush kelibsiz" << endl;
    */

    /*
    do {
     cout << "1: Qiymat berish\n2: Ko'paytirish\n3: Bo'lish\n4: Chiqish " << endl;
     cin >> op;
     switch (op) {
     case 1: cout << "m = "; cin >> m; cout << "n = "; cin >> n; break;
     case 2: cout << m << " * " << n << " = " << m * n << endl; break;
     case 3: cout << m << " / " << n << " = " << m / n << endl; break;
     case 4: break;
     }

    }while (op != 4);
     */
     /*
do{
   cout << "Hello world "; cin >> n;
}while (n >= 0);

cout << "Good bye world " << endl;
*/

    return 0;
}
