#include <iostream>

using namespace std;

int main()
{

    /*
 int time = 20;
 if(time < 18) {
    cout << "Good day." << endl;
 } else {
  cout << "Good evening " << endl;
 }
 */
 /*
 int a,b;
 cin >> a >> b;

 if (a < b) {
    cout << a << endl;
 }
 */


 /*
 int a,b,c;
 cin >> a >> b >> c;

 if((a > b) && (a > c)) {
    cout << "Eng katta son " << a << endl;
 } else if ((b > a) && (b > c)) {
   cout << "Eng katta son " << b << endl;
 } else if ((c > a) && (c > b)) {
   cout << "Eng katta son " << c << endl;
 } else if ((a == b) && (a == c)) {
   cout <<  "Hammasi teng " << a << " " << b << " " << c << endl;
 } else if ((a == b) && (c < a) && ( c < b)) {
   cout << "Katta sonlar " << a << " " << b << endl;
 } else if ((b == c) && (a < c) && (a < b)) {
   cout << "Katta sonlar " << b << " " << c << endl;
 } else if ((c == a) && (b < a) && (b < c)) {
   cout << "Katta sonlar " << c << " " << a << endl;
 }
 */


 /*
 int a,b;
 cin >> a;
 if ((a == 3) || (a > 8)) {
    cout << 8 << endl;
 } else {
   cout << 3 << endl;
 }
*/
/*
int a;
cin >> a;
cout << (11 - a);

    return 0;
}
