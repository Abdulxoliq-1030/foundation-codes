#include <iostream>

using namespace std;

int main()
{
    /*
    int a,b;
    cin >> a >> b;
    if (a > b){
        cout << a << endl;
    }else {
     cout << b << endl;
    }
    */

    /*
    int a,b;
    cin >> a >> b;
    cout << (a > b) * a + (b > a) * b << endl;
    */

    /*
    int a;
    cin >> a;
    a = (a > 1) && (a < 10) * 1
    */

    int a,b,c;
    return 0;


}

